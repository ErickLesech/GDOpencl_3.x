Notes : après avoir cloner le projet dans votre PC local faites les commandes suivantes

-Dans godot-cpp/
  scons platform=windows generate_bindings=yes -j4 bits=64

-Dans la racine du projet

  scons platform=windows -j4
